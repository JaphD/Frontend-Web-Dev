var mVar // mVar hold no value.
console.log(mVar);

mVar = null;
console.log(mVar); //mVar holds the value null

console.log('');

// Practice Problem
// 1. var mNum; console.log(mNum) - nNum holds no value or it is undefined
// 2. var mNum = null; console.log(mNum) - nNum holds a null value 

// 3. console.log(undefined == null) - '==' operator compares two values for equality after converting both values to a common type (type coercion).
// 4. console.log(undefined === null) - '===' operator compares both the value and the type without performing any type conversion.
